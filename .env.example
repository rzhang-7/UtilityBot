# Copy this file to `.env` and fill in the values. Do NOT commit your real token.
# Example environment variables for running the bot locally.
# Keep this file in repo (it contains no secrets).

DISCORD_TOKEN=your-bot-token-here

# Optional settings you can add to .env
# LOG_LEVEL=INFO
